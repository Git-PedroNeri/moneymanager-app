import { FormGroup } from "@angular/forms";

export function nomeEmailIguaisValidator(formGroup: FormGroup) {
  // const nome = formGroup.get('nomeOrigem')?.value ?? " ";
  // const email = formGroup.get('email')?.value ?? " ";

  // if (nome.trim() + email.trim()) {
  //   return nome !== email ? null : { nomeIgualEmail: true };
  // } else {
  //   return null;
  // }
  return null
}
